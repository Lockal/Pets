const acc = document.querySelector('.upper__lang-select');

const handleClick = () => document.querySelector('.upper__lang-select-list').style.display = "flex";

acc.addEventListener('click', handleClick);

